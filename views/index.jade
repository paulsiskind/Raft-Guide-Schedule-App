extends layout.jade

block content
  if (!user)
   button(type="fancy")
    a(href="/login") Login
    br
   button(type="fancier")
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}

    h1 Down Stream Adventures
  
    button(type="subversion")
      a(href='/home') Customer Portal
    button(type="subversion")  
      a(href='/albums') Guideline
    button(type="taco")  
      a(href="/logout") Logout











